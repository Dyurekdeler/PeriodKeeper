<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/welcomeFragment">
    <fragment
        android:id="@+id/addPeriodFragment"
        android:name="com.example.periodkeeper.AddPeriodFragment"
        android:label="fragment_add_period"
        tools:layout="@layout/fragment_add_period" />
    <fragment
        android:id="@+id/welcomeFragment"
        android:name="com.example.periodkeeper.WelcomeFragment"
        android:label="fragment_welcome"
        tools:layout="@layout/fragment_welcome" >
        <action
            android:id="@+id/action_welcomeFragment_to_addPeriodFragment"
            app:destination="@id/addPeriodFragment" />
    </fragment>
</navigation>